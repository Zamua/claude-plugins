{
  "name": "zamua",
  "owner": {
    "name": "Zamua"
  },
  "description": "Zamua's personal Claude Code plugin marketplace.",
  "plugins": [
    {
      "name": "agent-board",
      "source": "./plugins/agent-board",
      "description": "Issue-board-driven orchestration of local Claude Code worker agents: label an issue (sources: GitHub or Linear) and a local poller spawns a worker in a pane (runtimes: tmux or herdr) that works in a per-issue worktree and opens a draft PR; you review and merge."
    },
    {
      "name": "telegram",
      "source": "./plugins/telegram",
      "description": "Telegram forum-topics channel: multiplex ONE bot token across MANY concurrent Claude sessions, one foreground Claude per Telegram topic, via a local proxy. Drop-in for the single-session telegram channel."
    },
    {
      "name": "reexplain",
      "source": "./plugins/reexplain",
      "description": "Re-explain what you just said, for a reader who is not a domain expert. Rebuilds the explanation from first principles instead of compressing it: defines each term on first use, keeps the domain's own vocabulary, runs one concrete example all the way through, and separates what is settled from what is broken from what nobody has decided."
    },
    {
      "name": "pr-review-board",
      "source": "./plugins/pr-review-board",
      "description": "React to a pull request with an emoji and a background Claude reviews it: a local poller spawns one review agent per changeset in its own herdr workspace, with a plain-English report live in nvim beside the agent, linked to the code it calls out. It ends by proposing a numbered list of comments and posts only the ones you pick. Never approves, never pushes."
    },
    {
      "name": "briefme",
      "source": "./plugins/briefme",
      "description": "Write a very short brief on the state of the current work: one paragraph on what is going on, then only the questions and decisions that need the user. Use when the user has lost the thread of a long session, is returning after a break, or wants to know what needs them before doing anything else."
    }
  ]
}
